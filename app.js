(function(){const D=window.CHITRAMITRA;const grid=document.getElementById('grid'),lang=document.getElementById('language'),age=document.getElementById('age'),search=document.getElementById('search'),count=document.getElementById('count');let fmt='all';function cards(){const q=search.value.trim().toLowerCase();const l=lang.value,a=age.value;let rows=[];D.topics.forEach(([id,n],ti)=>Object.keys(D.formats).forEach((f,fi)=>{const resourceAge=D.ages[(ti+fi)%D.ages.length];if(a!=='all'&&resourceAge!==a)return;if(fmt!=='all'&&f!==fmt)return;if(q&&!([id,n.en,n.te,n.hi,n.ta,n.kn,n.ml,D.formats[f].en,D.formats[f].label[l]].join(' ').toLowerCase().includes(q)))return;rows.push({id,n,f,age:resourceAge})}));count.textContent=`${rows.length} resources`;grid.innerHTML=rows.slice(0,80).map(x=>{const title=x.n[l]+' — '+D.formats[x.f].label[l];const url=`resources/${l}/${x.id}/${x.f}.html`;return `<article class="resource-card"><span class="tag">${D.formats[x.f].label[l]}</span><h3>${title}</h3><p>Printable ${D.formats[x.f].en.toLowerCase()} for children, available in English and Indian languages.</p><div class="meta"><span>${D.languages[l].native}</span><span>${x.age}</span></div><a href="${url}">Preview & print →</a></article>`}).join('')||'<p>No resources match those choices yet.</p>'}document.querySelectorAll('.filters button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');fmt=b.dataset.format;cards()});[lang,age,search].forEach(x=>x.addEventListener('input',cards));document.getElementById('searchBtn').onclick=()=>{document.getElementById('resources').scrollIntoView({behavior:'smooth'});cards()};const tl=document.getElementById('topicList');tl.innerHTML=D.topics.map(([id,n])=>`<a href="#resources" data-topic="${id}">${n[lang.value]}</a>`).join('');lang.addEventListener('change',()=>{tl.querySelectorAll('a').forEach(a=>{const t=D.topics.find(x=>x[0]===a.dataset.topic);a.textContent=t[1][lang.value]});});cards()})();